
@keyframes rotate
  from
    transform: rotateX(0)
  to
    transform: rotateX(150deg)


ul
  margin: 0
  li
    list-style: none
    a
      display: block
      text-decoration: none
      padding: 5px 10px
      &:hover
        text-decoration: underline
